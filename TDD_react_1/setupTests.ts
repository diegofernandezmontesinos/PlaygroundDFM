import { expect } from 'vitest'; // Importa expect de Vitest
import matchers from '@testing-library/jest-dom/matchers'; // Importa los matchers de jest-dom

expect.extend(matchers); // Extiende expect con los matchers de jest-dom


